#include <stdio.h>

/**
  * main - is the starting point for program execution
  *
  * Return: ends the execution of a function andreturns conrol to calling fn
  */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
		printf("%d", i);
	printf("\n");
	return (0);
}


